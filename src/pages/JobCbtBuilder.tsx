import { useState } from "react";
import { Link, useParams } from "react-router-dom";
import { Plus, Trash2 } from "lucide-react";
import { useHiringJob, useSaveCbtQuestions } from "../hooks/useHiring";
type Draft={questionText:string;choices:string[];correctChoice:string};
const blank=():Draft=>({questionText:"",choices:["","","",""],correctChoice:""});
export function JobCbtBuilder(){const {id}=useParams();const {data:job}=useHiringJob(id);const save=useSaveCbtQuestions(id||"");const [rows,setRows]=useState<Draft[]>([blank()]);function patch(i:number,next:Partial<Draft>){setRows(rows.map((r,n)=>n===i?{...r,...next}:r))}async function submit(e:React.FormEvent){e.preventDefault();const clean=rows.map(r=>({...r,questionText:r.questionText.trim(),choices:r.choices.map(x=>x.trim()).filter(Boolean)})).filter(r=>r.questionText&&r.choices.length>=2&&r.correctChoice);if(clean.some(r=>!r.choices.includes(r.correctChoice)))return;await save.mutateAsync(clean);}return <div className="page-stack"><section className="page-hero"><div><p className="eyebrow">Employer assessment builder</p><h1>{job?.title||"Role"} CBT</h1><p>Create role-specific multiple-choice questions. Candidates never receive the stored answer key; scoring happens on the server.</p></div></section><form onSubmit={submit} className="space-y-3">{rows.map((row,i)=><section key={i} className="rounded-2xl border border-paper-dim bg-white p-4 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold">Question {i+1}</h2>{rows.length>1&&<button type="button" onClick={()=>setRows(rows.filter((_,n)=>n!==i))} className="rounded-full p-2 text-flag"><Trash2 size={16}/></button>}</div><textarea required rows={2} value={row.questionText} onChange={e=>patch(i,{questionText:e.target.value})} placeholder="Question" className="mt-3 w-full rounded-xl border p-3"/><div className="mt-3 grid gap-2 sm:grid-cols-2">{row.choices.map((choice,n)=><div key={n} className="flex items-center gap-2"><input required value={choice} onChange={e=>{const choices=[...row.choices];choices[n]=e.target.value;patch(i,{choices});}} placeholder={"Option "+(n+1)} className="min-w-0 flex-1 rounded-xl border p-3"/><input type="radio" name={"correct-"+i} checked={!!choice&&row.correctChoice===choice} onChange={()=>patch(i,{correctChoice:choice})} title="Correct answer"/></div>)}</div><p className="mt-2 text-xs text-ink-faint">Select the radio button beside the correct option.</p></section>)}<button type="button" onClick={()=>setRows([...rows,blank()])} className="inline-flex items-center gap-2 rounded-xl border border-black/10 px-4 py-2.5 text-sm font-semibold"><Plus size={16}/>Add question</button>{save.error&&<p className="text-sm text-flag">{(save.error as Error).message}</p>}<div className="sticky bottom-20 rounded-2xl bg-white/95 p-3 shadow-xl backdrop-blur sm:bottom-4"><button disabled={save.isPending} className="w-full rounded-xl bg-ink px-4 py-3 font-semibold text-white disabled:opacity-40">{save.isPending?"Saving assessment…":"Save CBT questions"}</button><Link to="/organizations/manage" className="mt-2 block text-center text-xs font-semibold text-brand-dark">Back to hiring workspace</Link></div></form></div>}