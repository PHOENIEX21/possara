import { useState } from "react";
import { Navigate, useNavigate } from "react-router-dom";
import { Building2, ShieldCheck } from "lucide-react";
import { useAuth } from "../store/auth";
import { useRegisterOrganization } from "../hooks/useHiring";
const CATEGORIES=["Hotels & Hospitality","Education & Schools","Health","Technology","Finance","NGO & Foundation","Government & Public Institution","Religious Organization","Recreation & Tourism","Construction & Engineering","Media & Creative","Retail & Commerce","Professional Services"];
export function OrganizationRegister(){const {userId}=useAuth();const nav=useNavigate();const create=useRegisterOrganization();const [form,setForm]=useState({name:"",category:CATEGORIES[0],location:"",description:"",logoUrl:""});if(!userId)return <Navigate to="/signin" replace/>;async function submit(e:React.FormEvent){e.preventDefault();const org=await create.mutateAsync(form);nav(`/organizations/${org.slug}`);}return <div className="page-stack"><section className="page-hero"><div className="page-icon bg-brand-light text-brand-dark"><Building2 size={22}/></div><div><p className="eyebrow">Organization account</p><h1>Register your organization</h1><p>Create the real POSSARA profile your business, school, NGO or institution will use to publish roles and manage applicants.</p></div></section><form onSubmit={submit} className="rounded-3xl border border-paper-dim bg-white p-5 shadow-card sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Organization name<input required maxLength={120} value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-1 w-full rounded-xl border border-black/15 px-3 py-2.5 outline-none focus:border-brand"/></label><label className="text-sm font-semibold">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-1 w-full rounded-xl border border-black/15 px-3 py-2.5 outline-none focus:border-brand">{CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold sm:col-span-2">Location<input required placeholder="e.g. Ilorin, Kwara" value={form.location} onChange={e=>setForm({...form,location:e.target.value})} className="mt-1 w-full rounded-xl border border-black/15 px-3 py-2.5 outline-none focus:border-brand"/></label><label className="text-sm font-semibold sm:col-span-2">Logo URL <span className="font-normal text-ink-faint">(optional for now)</span><input type="url" value={form.logoUrl} onChange={e=>setForm({...form,logoUrl:e.target.value})} className="mt-1 w-full rounded-xl border border-black/15 px-3 py-2.5 outline-none focus:border-brand"/></label><label className="text-sm font-semibold sm:col-span-2">About the organization<textarea required rows={4} maxLength={800} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1 w-full resize-none rounded-xl border border-black/15 px-3 py-2.5 outline-none focus:border-brand"/></label></div><div className="mt-5 flex items-start gap-2 rounded-2xl bg-opportunity-light p-3 text-sm text-ink-light"><ShieldCheck size={18} className="mt-0.5 shrink-0 text-opportunity-dark"/><p>Registration creates the profile immediately. Job publishing remains in draft until POSSARA verifies the organization, protecting applicants from impersonation and scam listings.</p></div>{create.error&&<p className="mt-3 text-sm text-flag">{(create.error as Error).message}</p>}<button disabled={create.isPending} className="mt-5 w-full rounded-xl bg-ink px-5 py-3 font-semibold text-white disabled:opacity-50">{create.isPending?"Creating organization…":"Create organization profile"}</button></form></div>}