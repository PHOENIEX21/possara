import { useState } from "react";
import { Navigate, useNavigate } from "react-router-dom";
import { ArrowLeft, ArrowRight, Building2, Check, ShieldCheck } from "lucide-react";
import { useAuth } from "../store/auth";
import { useRegisterOrganization } from "../hooks/useHiring";
const CATEGORIES=["Hotels & Hospitality","Education & Schools","Health","Technology","Finance","NGO & Foundation","Government & Public Institution","Religious Organization","Recreation & Tourism","Construction & Engineering","Media & Creative","Retail & Commerce","Professional Services"];
export function OrganizationRegister(){const {userId}=useAuth();const nav=useNavigate();const create=useRegisterOrganization();const [step,setStep]=useState(0);const [done,setDone]=useState<any>(null);const [form,setForm]=useState({name:"",category:CATEGORIES[0],location:"",description:"",logoUrl:""});if(!userId)return <Navigate to="/signin" replace/>;async function submit(){const org=await create.mutateAsync(form);localStorage.setItem("possara-active-organization",org.id);window.dispatchEvent(new Event("possara-acting-identity-change"));setDone(org);}if(done)return <div className="page-stack"><section className="rounded-3xl border border-paper-dim bg-white p-8 text-center shadow-card"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-brand-light text-brand-dark"><Check size={26}/></span><h1 className="mt-4 text-2xl font-bold">{done.name} is ready</h1><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-ink-light">Your organization profile is live. You can post updates, publish roles, manage applicants and build the team now. Verification only controls the POSSARA verified-organization trust badge.</p><div className="mt-6 grid gap-2 sm:grid-cols-3"><button onClick={()=>nav("/organizations/"+done.id+"/jobs/new")} className="min-h-12 rounded-xl bg-brand px-5 py-3 text-sm font-bold text-white">Post first role</button><button onClick={()=>nav("/organizations/"+done.slug)} className="min-h-12 rounded-xl bg-ink px-5 py-3 text-sm font-semibold text-white">Open organization profile</button><button onClick={()=>nav("/organizations/manage")} className="min-h-12 rounded-xl bg-paper px-5 py-3 text-sm font-semibold">Hiring workspace</button></div></section></div>;return <div className="page-stack"><section className="page-hero"><div className="page-icon bg-brand-light text-brand-dark"><Building2 size={22}/></div><div><p className="eyebrow">Organization account</p><h1>Register your organization</h1><p>Set up a credible POSSARA organization profile in three clear steps.</p></div></section><div className="rounded-3xl border border-paper-dim bg-white p-5 shadow-card sm:p-7"><div className="mb-6 flex gap-2">{["Identity","About","Review"].map((x,i)=><span key={x} className={"rounded-full px-3 py-1.5 text-xs font-semibold "+(i===step?"bg-ink text-white":i<step?"bg-brand-light text-brand-dark":"bg-paper text-ink-faint")}>{i+1}. {x}</span>)}</div>{step===0&&<div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Organization name<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Category<select value={form.category} onChange={e=>setForm({...form,category:e.target.value})} className="mt-1 w-full rounded-xl border p-3">{CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold sm:col-span-2">Location<input value={form.location} onChange={e=>setForm({...form,location:e.target.value})} placeholder="City, State / Region" className="mt-1 w-full rounded-xl border p-3"/></label></div>}{step===1&&<div className="space-y-4"><label className="block text-sm font-semibold">About the organization<textarea rows={6} maxLength={800} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label><label className="block text-sm font-semibold">Logo URL <span className="font-normal text-ink-faint">(optional)</span><input type="url" value={form.logoUrl} onChange={e=>setForm({...form,logoUrl:e.target.value})} className="mt-1 w-full rounded-xl border p-3"/></label></div>}{step===2&&<div className="space-y-4"><div className="rounded-2xl bg-paper p-5"><p className="eyebrow">Review</p><h2 className="mt-1 text-xl font-bold">{form.name||"Organization"}</h2><p className="mt-1 text-sm text-ink-light">{form.category} · {form.location}</p><p className="mt-4 whitespace-pre-wrap text-sm leading-6">{form.description||"No description yet."}</p></div><div className="flex gap-2 rounded-2xl bg-opportunity-light p-3 text-sm text-ink-light"><ShieldCheck size={18} className="shrink-0 text-opportunity-dark"/><p>The profile is created immediately and can publish updates and roles. POSSARA verification adds the verified-organization trust badge after review; it does not block normal hiring.</p></div>{create.error&&<p className="text-sm text-flag">{(create.error as Error).message}</p>}</div>}<div className="mt-7 flex justify-between border-t border-paper-dim pt-4"><button type="button" onClick={()=>step?setStep(step-1):nav("/organizations")} className="inline-flex items-center gap-1 rounded-xl px-4 py-2.5 text-sm font-semibold"><ArrowLeft size={15}/>{step?"Back":"Cancel"}</button>{step<2?<button type="button" disabled={step===0&&(!form.name.trim()||!form.location.trim())} onClick={()=>setStep(step+1)} className="inline-flex items-center gap-1 rounded-xl bg-ink px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-40">Continue <ArrowRight size={15}/></button>:<button type="button" disabled={create.isPending||!form.description.trim()} onClick={submit} className="rounded-xl bg-brand px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{create.isPending?"Creating…":"Create organization"}</button>}</div></div></div>}