import { useState } from "react";
import { supabase } from "../lib/supabase";
import { useAuth } from "../store/auth";

export function Settings(){const {email}=useAuth();const [currentPassword,setCurrentPassword]=useState("");const [newPassword,setNewPassword]=useState("");const [confirmPassword,setConfirmPassword]=useState("");const [error,setError]=useState<string|null>(null);const [success,setSuccess]=useState(false);const [loading,setLoading]=useState(false);async function handleChangePassword(e:React.FormEvent){e.preventDefault();setError(null);setSuccess(false);if(newPassword!==confirmPassword){setError("New passwords don't match.");return;}if(newPassword.length<8){setError("New password must be at least 8 characters.");return;}if(!email){setError("Couldn't determine your account email.");return;}setLoading(true);const {error:verifyError}=await supabase.auth.signInWithPassword({email,password:currentPassword});if(verifyError){setLoading(false);setError("Current password is incorrect.");return;}const {error:updateError}=await supabase.auth.updateUser({password:newPassword});setLoading(false);if(updateError){setError(updateError.message);return;}setSuccess(true);setCurrentPassword("");setNewPassword("");setConfirmPassword("");}return <div className="max-w-prose"><h1 className="text-2xl">Settings</h1><p className="mt-1 text-ink-light">Account, privacy, notifications, preferences and security are being consolidated here.</p><section className="mt-8 border-t border-paper-dim pt-6"><h2 className="text-lg">Account</h2><p className="mt-1 text-sm text-ink-light">Signed in as {email}</p></section><section className="mt-8 border-t border-paper-dim pt-6"><h2 className="text-lg">Security</h2><p className="mt-1 text-sm text-ink-light">Changing your password requires your current password.</p><form onSubmit={handleChangePassword} className="mt-4 max-w-sm space-y-4">{[["Current password",currentPassword,setCurrentPassword],["New password",newPassword,setNewPassword],["Confirm new password",confirmPassword,setConfirmPassword]].map(([label,value,setter],i)=><label key={label as string} className="block text-sm text-ink-light">{label as string}<input type="password" required minLength={i===0?1:8} value={value as string} onChange={e=>(setter as (v:string)=>void)(e.target.value)} className="mt-1 w-full rounded-lg border border-ink-faint/30 px-3 py-2 text-ink outline-none focus:border-brand"/></label>)}{error&&<p className="text-sm text-flag">{error}</p>}{success&&<p className="text-sm text-trust-dark">Password updated.</p>}<button type="submit" disabled={loading} className="rounded-full bg-ink px-5 py-2 text-sm font-medium text-white disabled:opacity-50">{loading?"Updating…":"Update password"}</button></form></section></div>}
