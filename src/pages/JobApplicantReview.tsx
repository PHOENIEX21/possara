import { Link, useParams } from "react-router-dom";
import { ArrowLeft, Download, MessageCircle, ShieldCheck, UserRound } from "lucide-react";
import { useApplicationReview, useUpdateJobApplication } from "../hooks/useHiring";
import { supabase } from "../lib/supabase";
import { useState } from "react";
export function JobApplicantReview(){const {id,applicationId}=useParams();const {data,isLoading,error}=useApplicationReview(applicationId);const update=useUpdateJobApplication(id||"");const [cvError,setCvError]=useState("");if(isLoading)return <div className="feed-skeleton"/>;if(error||!data)return <div className="empty-state"><h1>Applicant unavailable</h1><p>Only the organization owner or recruiter for this role can review this application.</p></div>;const {app,answers,questions,attempt}=data;const questionById=new Map(questions.map((q:any)=>[q.id,q.question_text]));async function openCv(){setCvError("");const {data:signed,error}=await supabase.storage.from("job-documents").createSignedUrl(app.cv_url,300);if(error||!signed?.signedUrl){setCvError("Could not securely open this CV.");return;}window.open(signed.signedUrl,"_blank","noopener,noreferrer");}return <div className="page-stack"><Link to={"/organizations/jobs/"+id+"/applicants"} className="inline-flex items-center gap-1.5 text-sm text-ink-light"><ArrowLeft size={15}/>Applicants</Link><section className="rounded-3xl border border-paper-dim bg-white p-5 shadow-card"><div className="flex items-start gap-3">{app.profiles?.avatar_url?<img src={app.profiles.avatar_url} alt="" className="h-14 w-14 rounded-full object-cover"/>:<div className="flex h-14 w-14 items-center justify-center rounded-full bg-paper-dim"><UserRound/></div>}<div className="min-w-0 flex-1"><h1 className="text-2xl font-bold">{app.profiles?.full_name||"POSSARA member"}</h1><p className="text-sm text-ink-faint">{app.profiles?.headline||app.profiles?.location||"Applicant"}</p></div><span className="rounded-full bg-paper-dim px-3 py-1 text-xs font-bold capitalize">{app.status}</span></div>{app.profiles?.skills?.length>0&&<p className="mt-4 text-sm text-ink-light"><b>Skills:</b> {app.profiles.skills.join(" · ")}</p>}<div className="mt-4 flex flex-wrap gap-2"><button onClick={()=>void openCv()} className="inline-flex items-center gap-2 rounded-xl bg-ink px-3 py-2 text-xs font-semibold text-white"><Download size={14}/>Open CV securely</button><Link to={"/messages/"+app.applicant_id} className="inline-flex items-center gap-2 rounded-xl bg-brand-light px-3 py-2 text-xs font-semibold text-brand-dark"><MessageCircle size={14}/>Message applicant</Link></div>{cvError&&<p className="mt-2 text-xs text-flag">{cvError}</p>}</section><section className="rounded-3xl border border-paper-dim bg-white p-5 shadow-card"><p className="eyebrow">Application</p><h2 className="text-xl font-bold">Screening responses</h2>{app.cover_letter_text&&<div className="mt-4 rounded-2xl bg-paper p-4"><p className="text-xs font-bold uppercase tracking-wide text-ink-faint">Cover letter</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6">{app.cover_letter_text}</p></div>}<div className="mt-3 space-y-2">{answers.filter((a:any)=>a.question_kind==="screening").map((a:any)=><div key={a.question_id} className="rounded-2xl border border-paper-dim p-3"><p className="text-xs font-semibold text-ink-faint">{questionById.get(a.question_id)||"Screening question"}</p><p className="mt-1 text-sm font-medium">{a.answer_value||"No answer"}</p></div>)}</div>{!answers.length&&<p className="mt-3 text-sm text-ink-faint">No screening responses were recorded.</p>}</section>{app.job_postings?.requires_cbt&&<section className="rounded-3xl border border-paper-dim bg-white p-5 shadow-card"><div className="flex items-center gap-2"><ShieldCheck size={18}/><h2 className="font-bold">CBT assessment</h2></div>{attempt?.submitted_at?<p className="mt-3 text-3xl font-bold">{attempt.score}% <span className="text-sm font-normal text-ink-faint">submitted {new Date(attempt.submitted_at).toLocaleDateString()}</span></p>:<p className="mt-3 text-sm text-ink-faint">Candidate has not submitted the CBT yet.</p>}</section>}<section className="rounded-3xl border border-paper-dim bg-white p-4 shadow-card"><p className="mb-3 text-sm font-semibold">Hiring decision for this role</p><div className="flex flex-wrap gap-2"><button onClick={()=>update.mutate({id:app.id,status:"shortlisted"})} className="rounded-xl bg-trust-light px-4 py-2 text-sm font-semibold text-trust-dark">Shortlist</button><button onClick={()=>update.mutate({id:app.id,status:"hired"})} className="rounded-xl bg-ink px-4 py-2 text-sm font-semibold text-white">Mark hired</button><button onClick={()=>update.mutate({id:app.id,status:"rejected"})} className="rounded-xl bg-red-50 px-4 py-2 text-sm font-semibold text-flag">Not selected</button></div></section></div>}