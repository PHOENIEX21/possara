import { BadgeCheck } from "lucide-react";
export function OrganizationVerificationBadge({compact=false}:{compact?:boolean}){return <span title="Verified organization · POSSARA has reviewed this organization identity. Verification does not guarantee a job, payment, or third-party offer." aria-label="Verified organization identity" className={`inline-flex shrink-0 items-center gap-1 rounded-full border border-trust/20 bg-trust-light font-semibold text-trust-dark ${compact?"px-1.5 py-0.5 text-[10px]":"px-2 py-1 text-xs"}`}><BadgeCheck size={compact?18:14}/>{!compact&&<span>Verified organization</span>}</span>}